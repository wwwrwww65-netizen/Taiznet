"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
// @ts-ignore
var magazine_base_url = hotspotConfig['magazine-base-url'];
// @ts-ignore
var magazine_names = hotspotConfig['magazine-names'];
if (magazine_base_url == null || magazine_base_url === "")
    magazine_base_url = "magazine/";
if (magazine_base_url !== "magazine/" && !magazine_base_url.includes('http'))
    magazine_base_url = "http://" + magazine_base_url;
//console.log(magazine_base_url)
var d = 1;
/*function delay(ms) {
    return new Promise(function(resolve) {
        setTimeout(resolve, ms);
    });
}*/
/*function checkImageFound() {
    return new Promise(function(resolve) {
        resolve();
    });
}*/
function asyncAwait() {
    return __awaiter(this, void 0, void 0, function () {
        return __generator(this, function (_a) {
            for (d; 20 > d; d++) {
                e();
                /*await checkImageFound();*/
            }
            return [2 /*return*/];
        });
    });
}
function f(url, b) {
    var icon = url + "icon/" + b + ".jpg";
    var videoUrl = "" + url + b + ".mp4";
    var divId = "magazine_".concat(b);
    var div = document.createElement('div');
    div.classList.add('magazine-grid');
    div.style.backgroundImage = "url(".concat(icon, ")");
    div.id = divId;
    document.getElementById("magazine-grid").innerHTML += div.outerHTML;
    setTimeout(function () {
        document.getElementById(divId).addEventListener('click', function (e) {
            var video = document.createElement('video');
            var player = document.getElementById('player');
            if (!video.canPlayType('video/mp4')) {
                player.innerText = "للاسف!! متصفحك قديم جدا ولايدعم تشغيل الفيديو, يرجى تحديث المتصفح او استخدام متصفح اخر لتتمكن من المشاهدة";
                return;
            }
            video.poster = icon;
            video.src = videoUrl;
            video.autoplay = true;
            video.controls = true;
            video.muted = false;
            //video.height = 240; // 👈️ in px
            //video.width = 320; // 👈️ in px
            video.style.width = "100%";
            var magazine = document.getElementById('magazine');
            magazine.style.display = "none";
            magazine.classList.remove("active");
            // @ts-ignore
            showParentElement(null, document.getElementById('magazine-player'));
            player.innerHTML = video.outerHTML;
            //video.play()
            video.pause();
        });
    }, 200);
}
function e() {
    var b = d, a = new Image, url = magazine_base_url;
    a.src = url + "icon/" + b + ".jpg";
    a.complete ? (f(url, b)) : (a.onload = function () {
        f(url, b);
    }, a.onerror = function () {
        console.log("no img");
    });
    if (magazine_base_url !== "magazine/") {
        setTimeout(function () {
            if (!a.complete || !a.naturalWidth) {
                url = "magazine/";
                a.src = url + "icon/" + b + ".jpg";
            }
        }, 3000);
    }
}
asyncAwait();
